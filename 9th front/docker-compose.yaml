version: '3'

networks:
  app-net:
    external: true
    name: app

services:
  frontend9:
    image: wss:front9
    container_name: frontend9
    ports:
      - 3009:3000
    networks:
      - app-net
    env_file:
      - .env
